<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multi.travel.mapper.TravelMapper">

    <sql id="searchCondition">
        <if test="keyword != null and keyword != ''">
            WHERE title LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%')
            OR address LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </sql>

    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM travel
        <include refid="searchCondition"/>
    </select>

    <select id="findAllPaged" resultType="com.multi.travel.model.Travel">
        SELECT no, district, title, description, address, phone, latitude, longitude
        FROM travel
        <include refid="searchCondition"/>
        ORDER BY no
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" parameterType="int" resultType="com.multi.travel.model.Travel">
        SELECT no, district, title, description, address, phone, latitude, longitude
        FROM travel
        WHERE no = #{no}
    </select>

</mapper>